doc: Build all available OpenDS backends with meson on the target (working tree must be synced first).

steps:
  - name: build
    run: |
      '{{ config.test.repo_path }}/tasks/steps/build_opends.sh' '{{ config.test.repo_path }}' '{{ config.test.build_dir }}'
